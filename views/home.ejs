<%- include('partials/header'); -%>


    <h1>Home</h1>

    <p>
        <%= startingContent %>
    </p>

    <% for( let i=0; i < posts.length; i++ ) { %>
        <h1>
            <%= posts[i].title%>
        </h1>
        <p>
            <%= posts[i].content.slice(0, 10)+"..."%> <a href="/posts/<%=posts[i]._id%>">Read more</a>
        </p>
        <% } %>

            <%- include('partials/footer'); -%>